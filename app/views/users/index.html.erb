
<h3 align="center">Chat up your robo match!</h3>

<hr>
<div class="container">
  <div class="row">
    <% @users.each do |user| %>
      <div class="col-md-4">
        <% if user.id != current_user.id %>
        <h5>Check out <%=link_to "#{user.name}", user_path(user) %>'s profile</h5>
        <% end %>
      </div>
      <% end %>
``  </div>
  </div>
</div>
